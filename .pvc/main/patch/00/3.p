--- cmd/pvc/README.cxt
+++ cmd/pvc/README.cxt
@@ -49 +49 @@
-    directory. If an id is given then instead move ids from [id,tip]
+    directory. If an id is given then instead move ids from [$[id,tip]]
